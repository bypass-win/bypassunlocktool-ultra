[build]
  command = "vite build --config vite.netlify.config.ts"
  publish = "dist/client"

# TanStack Start server functions, SSR, server routes, and the
# /api/public/nowpayments-webhook are mapped to Netlify Functions
# automatically by @netlify/vite-plugin-tanstack-start. No redirects needed.
